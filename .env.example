# Copy to .env and fill the key for the provider you selected in `orvena init`.
# Never commit .env. Orvena reads only the key for your chosen provider.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=
# `ollama` and `offline` providers need no key.
